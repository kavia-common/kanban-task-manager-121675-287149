# Linting and Formatting

- Run lint: `npm run lint`
- Autofix: `npm run lint:fix`
- Format: `npm run format`

ESLint uses flat config (eslint.config.mjs) with React rules and Prettier-compatible settings. Ignored paths: node_modules, build, coverage, public.
